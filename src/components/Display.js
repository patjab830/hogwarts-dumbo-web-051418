import React from 'react'

export default class Display extends React.Component {
  render() {
    const currentHogObj = this.props.currentHogObj
    console.log(this.props.currentHogObj)
    return (<div>

    </div>
    )
  }
}
